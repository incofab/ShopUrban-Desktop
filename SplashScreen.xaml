<Window x:Class="ShopUrban.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShopUrban"
        mc:Ignorable="d"
        Title="SplashScreen" Height="350" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        >
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <Image Source="./logo.ico" Width="150" VerticalAlignment="Center" HorizontalAlignment="Center"
                   />
            <TextBlock Text="Welcome to Shop Urban" VerticalAlignment="Center" 
                       HorizontalAlignment="Center" Margin="0 20 0 0"  FontSize="20"
                       Foreground="{StaticResource PrimaryHueMidBrush}" FontWeight="DemiBold"/>
        </StackPanel>
    </Grid>
</Window>
